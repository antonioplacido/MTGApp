const initialState = {
  status: "idle",
  error: null,
};

export default function authReducer(state = initialState, action) {
  // const stateCopy = { ...state };
  switch (action.type) {
    default:
      return state;
  }
}
